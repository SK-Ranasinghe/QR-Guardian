| Test Case ID | Test Case | Expected Outcome | Actual Outcome | Status |
|---|---|---|---|---|
| T01 | Biometric authentication succeeds on a supported and enrolled device | The biometric prompt should complete successfully and the main app stack should render | The biometric check completed successfully and the app opened to the main interface instead of the lock screen | Pass |
| T02 | Biometric authentication fails after an unsuccessful user attempt | Access should remain blocked and the lock screen should remain visible | After an unsuccessful biometric attempt, the app stayed locked and the lock screen remained visible | Pass |
| T03 | Device-level fallback is offered during biometric authentication | The system prompt should show device passcode fallback and allow continuation through device security | The authentication prompt offered the option to continue using the device passcode, providing a backup way to access the app | Pass |
| T04 | Device has no biometric hardware or no enrolled biometric profile | The app should permit access instead of blocking startup | On devices without biometric support or without an enrolled biometric profile, the app opened normally instead of blocking access | Pass |
| T05 | Unexpected biometric runtime error occurs | The app should fail open and avoid locking the user out | When an unexpected biometric error occurred, the app still allowed access so the user was not locked out | Pass |
| T06 | Standard HTTPS URL is scanned | The payload should be intercepted, analyzed, saved to history, and forwarded to the result screen before any open action | After scanning a normal HTTPS link, the app analyzed it, saved the result to history, and showed the result screen before any link could be opened | Pass |
| T07 | `WIFI:` QR payload is scanned | The app should flag the network configuration, reduce the score, and prevent blind execution | The app recognized the QR code as Wi-Fi configuration data, warned the user about the network setup, lowered the trust score, and treated insecure Wi-Fi types as dangerous | Pass |
| T08 | Plain-text Wi-Fi sharing payload is scanned as two tokens | The app should heuristically recognize the payload as Wi-Fi sharing and apply a cautionary penalty | The app recognized the text as a likely Wi-Fi sharing payload, showed caution warnings, and significantly lowered the safety score | Pass |
| T09 | `SMSTO:` QR payload is scanned | The app should classify the payload as dangerous and show an SMS-triggering financial-risk warning | The app identified the QR code as an SMS-triggering payload, warned about possible financial risk, and rated it as dangerous | Pass |
| T10 | `TEL:` QR payload is scanned | The app should classify the payload as a privacy-risk action and not open it automatically | The app identified the QR code as a phone-call action, warned about the privacy risk, and did not allow it to open automatically | Pass |
| T11 | High-entropy domain is scanned | The heuristic engine should flag domain randomness and reduce the safety score | The suspicious-looking domain was flagged as unusually random, and the safety score was reduced accordingly | Pass |
| T12 | Punycode domain beginning with `xn--` is scanned | The result should be marked as a homograph risk and forced into a dangerous classification | The punycode domain was treated as a possible look-alike attack, marked as dangerous, and heavily penalized | Pass |
| T13 | URL contains sensitive keywords such as `login` or `verify` | The system should flag security-sensitive wording and reduce the score | Links containing words such as login or verify were treated as more sensitive and received a lower safety score | Pass |
| T14 | Typosquatting against a known brand is detected | The URL should be flagged as phishing and downgraded heavily | The brand-imitating address was flagged as a likely phishing attempt and given a strong safety penalty | Pass |
| T15 | Gemini returns a valid JSON response | The AI service should extract and parse the JSON and return a typed `GeminiAiInsight` object | The AI response was read successfully and shown as a clear structured assessment with a verdict, risk score, explanation, and threat type | Pass |
| T16 | Gemini returns a conversational response with no JSON block | The AI service should return a neutral fallback rather than crash the feature | When the AI replied in plain conversational text instead of the expected format, the app safely returned a neutral result instead of failing | Pass |
| T17 | Gemini returns malformed JSON inside the response | The parser should catch the error and return a neutral fallback result | When the AI response was badly formatted, the app handled it safely and returned a neutral fallback result | Pass |
| T18 | VirusTotal deep scan completes after polling | The app should poll the analysis endpoint, normalize the engine results, and display a summary | The deep scan completed successfully after checking for updates, and the app displayed a full summary of the security engines' findings | Pass |
| T19 | IP2Location returns a newly registered domain | The app should calculate `ageDays`, flag `isVeryNew`, and use the result in adjusted scoring | The domain information showed that the site was newly registered, and the app treated that as an additional risk factor in the final assessment | Pass |
| T20 | Google Safe Browsing detects a known threat | The result should append threat information and apply an additional score penalty | When a known threat was detected, the app added that warning to the result and lowered the score further | Pass |
| T21 | A scan result is saved to history | A new history item with ID, timestamp, score, rating, and issues should be persisted locally | The scan result was saved locally with its key details, and the newest record appeared at the top of the history list | Pass |
| T22 | History is cleared from local storage | The history key should be removed and subsequent reads should return an empty list | The saved history was removed successfully, and the next history check showed no stored records | Pass |
| T23 | Threat level changes between previous and current scans | A threat-update notification should be scheduled when a previously safer domain becomes more dangerous | When a previously scanned domain became more dangerous, the app prepared a threat update alert for the user | Pass |
| T24 | Risk-based open confirmation is triggered for low-confidence results | The result screen should show a warning modal before opening the payload | For lower-confidence results, the app showed a warning confirmation before allowing the user to continue | Pass |
| T25 | Result screen color-coding reflects the safety rating | SAFE, CAUTION, and DANGEROUS results should render with distinct visual colors and icons | The result screen used different colors and icons for safe, caution, and dangerous outcomes, making the status easy to understand | Pass |
